# API base URL for the backend (Node or FastAPI).
# Backend runs on port 4000 by default (Node: npm run dev in Backend/).
# Use this so the frontend knows where to send auth and API requests.
VITE_API_URL=http://localhost:4000
