PORT=4000
# Database in Atlas where users, scans, items are stored (Browse Collections → this name)
MONGODB_DB_NAME=coronet
# No database name in the path — only after ? — so dbName above is the single source of truth
MONGODB_URI=mongodb+srv://COROnet_user:database2025678@coronet.liazkv1.mongodb.net/?retryWrites=true&w=majority&appName=COROnet
JWT_SECRET=coronet-dev-only-replace-with-long-random-secret-for-production
CORS_ORIGIN=http://localhost:5173
